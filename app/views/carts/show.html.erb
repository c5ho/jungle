<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if enhanced_cart.length == 0 %>
    <%= render 'show_empty' %>
  <% else %>
    <%= render 'show_full' %>
  <% end %>


</section>
